name: slow
desc: run slow routine
type: general
validator: domain



routines:
  - modules:
      - subdomain

  - flow: 'slow'
    modules:
      - probing

  - modules:
      - screenshot
  - modules:
      - sto
      - fingerprint
  - flow: 'slow'
    modules:
      - spider
      - archive
  - modules:
      - googledorks
      - ipspace
  - modules:
      - vulnscan
  - modules:
      - vhostscan

  - flow: 'slow'
    modules:
      - portscan
  - flow: 'slow'
    modules:
      - pdirbscan

  - flow: 'slow'
    modules:
      - dirbscan
  # push final result again
  - modules:
      - summary
