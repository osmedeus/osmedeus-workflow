name: slow
desc: run slow routine
type: general
validator: domain



routines:
  - modules:
      - subdomain
  - flow: 'slow'
    modules:
      - probing
  - modules:
      - screenshot
  - modules:
      - sto
      - fingerprint
  - modules:
      - spider
      - archive
      - ipspace
  - modules:
      - vulnscan

  - flow: 'slow'
    modules:
      - portscan
  - flow: 'slow'
    modules:
      - pvulnscan
  - flow: 'slow'
    modules:
      - dirbscan
  # push final result again
  - modules:
      - summary
