name: extensive
desc: run extensive routine with more in-depth module
type: general
validator: domain

routines:
  - modules:
      - subdomain
  - flow: 'extensive'
    modules:
      - probing
  - modules:
      - screenshot
  - modules:
      - sto
      - fingerprint
  - modules:
      - spider
      - archive
      - ipspace
  - modules:
      - vulnscan

  - flow: 'extensive'
    modules:
      - portscan
  - flow: 'extensive'
    modules:
      - pvulnscan
  - flow: 'extensive'
    modules:
      - dirbscan
  # push final result again
  - modules:
      - summary
